<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="SearchViewStyle" parent="Widget.AppCompat.Light.SearchView">
        <item name="android:textColorHint">@color/white</item>
        <item name="android:queryBackground">@color/white</item>
        <item name="itemTextColor">@color/white</item>
        <item name="queryHint">@string/searchint</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>

    </style>
    <style name="SearchViewStyleList" parent="Widget.AppCompat.Light.SearchView">

        <item name="android:textColorHint">@color/black</item>
        <item name="android:queryBackground">@color/black</item>
        <item name="itemTextColor">@color/black</item>
        <item name="queryHint">@string/searchint</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/black</item>

    </style>


</resources>
